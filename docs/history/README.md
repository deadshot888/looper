# Project History

The original implementation prompt and launch-note draft were removed from the public root after the working product documentation superseded them. They remain available in Git history through release `0.2.0`.

Current behavior is documented in the root `README.md`, `SPEC.md`, and `ARCHITECTURE.md`.
